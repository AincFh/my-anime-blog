---
description: A.T. Field 项目 Cursor 快捷命令配置
alwaysApply: true
---

# Cursor 快捷命令配置

本文件定义了 A.T. Field 项目中可用的自定义命令。这些命令可以通过 Cursor 的命令菜单或快捷键触发。

## 可用命令

| 命令名称 | 描述 | 图标 |
|---------|------|------|
| /new-article | 创建新文章 | 📝 |
| /deploy | 部署项目 | 🚀 |
| /backup-db | 备份数据库 | 💾 |
| /new-component | 创建新组件 | 🎨 |
| /code-review | 代码审查 | 🔍 |

## 命令详细说明

### 1. /new-article - 创建新文章

快速创建一个新的 Markdown 文章模板。

**使用流程：**
1. 收集文章信息（标题、分类、标签）
2. 生成 Frontmatter 元数据
3. 创建文章模板文件
4. 生成 D1 INSERT 语句（如需要）

---

### 2. /deploy - 部署项目

执行完整的部署流程，包括构建和发布到 Cloudflare Workers。

**部署步骤：**
1. 部署前检查（lint、typecheck）
2. 构建项目
3. 本地测试（如需要）
4. 部署到 Cloudflare
5. 部署后验证

---

### 3. /backup-db - 备份数据库

创建 Cloudflare D1 数据库的备份并导出数据。

**备份选项：**
- 本地数据库备份
- 生产数据库备份
- 同时备份本地和生产

---

### 4. /new-component - 创建新组件

快速创建一个新的 UI 组件，包含玻璃态样式和完整功能。

**可创建的组件类型：**
- 按钮组件
- 卡片组件
- 输入框组件
- 模态框组件
- 导航栏组件
- 其他自定义组件

---

### 5. /code-review - 代码审查

对代码变更进行全面的安全和代码质量审查。

**审查范围：**
- SQL 注入检查
- XSS 跨站脚本检查
- 认证授权检查
- 敏感信息检查
- TypeScript 类型检查
- 代码规范检查

---

## 使用方法

在 Cursor 中打开命令面板（Ctrl+Shift+P 或 Cmd+Shift+P），输入命令名称即可触发对应的命令。
